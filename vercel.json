{
    "version": 2,
    "builds": [
      { "src": "app.js", "use": "@now/node", "config": { "maxLambdaSize": "15mb" } }
    ],
    "routes": [
      { "src": "/static/(.*)", "dest": "/public/$1" },
      { "src": "/(.*)", "dest": "/app.js" }
    ]
  }
  